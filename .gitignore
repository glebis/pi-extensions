# Symlinks into pi's own bundled dependencies (editor/tsc support only;
# pi resolves these packages itself at runtime).
node_modules/

# Generated by `herdr integration install pi` — managed by herdr, overwritten
# on reinstall, not source we author.
herdr-agent-state.ts

# OS / editor noise
.DS_Store
*.log
